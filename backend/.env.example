# Smart Budgeting Backend Configuration

# API Server
BACKEND_HOST=127.0.0.1
BACKEND_PORT=8000

# CORS Settings (allow Flutter app)
CORS_ORIGINS=http://localhost:*,http://127.0.0.1:*

# Note: When deploying to production (e.g., Render, Railway),
# update BACKEND_URL to your deployed backend URL
